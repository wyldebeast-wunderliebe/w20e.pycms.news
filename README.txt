w20e.pycms.sharing
==================

w20e.pycms sharing option for content. To use it in your PyCMS app,
add the egg dependency (usually in your buildout) and include the
package ZCML from your configure.zcml like so:

  <include package="w20e.pycms_sharing"/>
